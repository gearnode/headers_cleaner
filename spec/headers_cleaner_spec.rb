require "spec_helper"

describe HeadersCleaner do
  it "has a version number" do
    expect(HeadersCleaner::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
